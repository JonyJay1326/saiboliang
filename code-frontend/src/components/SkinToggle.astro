---
// 皮肤强度档控件（frontend-spec §5.9）—— 纯标记，无脚本。
// 位置：页面右上角；抽成独立组件，位置后续可能变（二期「桌宠计划」可能承接）。
// 行为（读写 localStorage、写回 <html>、按钮 aria 同步）统一由 Base.astro 的
// <head> 内联脚本托管：与 §7.2「我领到了」用同一套事件委托思路，
// 避免脚本随组件被 persist / 替换而产生重复绑定。
// 默认档=朴素档（2026-09-18 用户拍板）；各档强度取值待 UI 定稿（§11.1）——cyber/plain 为内部值。
---
<div class="skin-toggle">
  <button type="button" data-skin-set="cyber">赛博档</button>
  <button type="button" data-skin-set="plain">朴素档</button>
</div>
