# AGENTS.md — 赛博粮仓（saiboliang）协作约束

> 任何 AI 工具（Codex / openCode / WorkBuddy / Cursor）进入本目录干活前，先读完本文件。  
> 本文件是协作约束的唯一入口；修改本文件需经用户（小黑皮）确认。

## 1. 项目是什么

日更型「AI 白嫖机会 + 比价 + 榜单」聚合站。视觉气质：古风票证 × 赛博朋克。  
域名：**saiboliang.top**（NameSilo 注册 + Cloudflare NS）。

## 2. 唯一事实源

**本目录（D:\Code\ccode\saiboliang）是一切文档与代码的唯一权威版本。**  
其他工作区（如 `C:\Users\yingm\WorkBuddy\赛博粮票`）存在的同名文档均为历史快照，冲突时以本目录为准，不得反向同步覆盖本目录。

## 3. 目录角色（改动前先看这张表）

| 目录               | 角色                         | 约束                                        |
| ---------------- | -------------------------- | ----------------------------------------- |
| `doc-data/`      | 数据侧文档：架构 / 数据契约 / 审查稿      | 契约是冻结层，见 §4                               |
| `doc-front/`     | 前端文档（现状盘点已就位，设计未定稿）        | 只写结构与文字描述，不写色值/字号/CSS/动效                  |
| `demo-html/`     | 参考页面 + 字体 + 19 张 PNG 资产    | **仅风格参考，不作实现依据**                          |
| `code-backend/`  | 数据管道代码（采集脚本，只产 JSON 不提供服务） | 将发布到 GitHub，注意 §6 隐私红线                    |
| `code-frontend/` | 前端代码（Astro + Vue 岛）        | 将发布到 GitHub，注意 §6 隐私红线                    |
| `cache-tools/`   | 本地临时工具（如字体库生成图片脚本）         | **不部署、不发布**，只在本机用                         |
| `research/`      | 外部调研与证据留档（如 freeegg 逆向拆解）  | **事实参考，不是规格**；实现决定回到 doc-data / doc-front |

## 4. 契约冻结纪律

- `doc-data/cyber-granary-data-contract.md` 是前端与数据管道的**唯一接缝**。
- AI 可自主实现管道与前端，但**不得修改契约的字段名与字段语义**；确实需要新字段时，必须停下来向用户提出契约变更请求，由用户拍板。
- 前端可从契约字段派生只读展示标记，不得写回数据层。

## 5. 参考稿纪律

- `demo-html/` 下所有 HTML（含 claude.html、cyberpunk-grain-station-design.html）**只作风格参考，不是实现方案，不得照抄其结构与代码**。
- 前端实现依据 = `doc-front/` 下的文档；doc-front 没写的，先问用户，不要从参考稿反推规格。

## 6. 字体授权与隐私红线（高压线）

4 款喜鹊字体（乌冬面简体 / 在山林体简体版 / 招牌体 / 古字典体）**用户均持有授权证书**，但授权有边界：

1. **证书含真实姓名 + 手机号，绝不能提交进公开 GitHub 仓库**；证书文件一律留本机，不进 `code-backend/`、`code-frontend/`。
2. **个人商用授权不含 webfont 授权，子集化也不行** → 站内标题一律走「字体图片化」（预渲染 PNG），**禁止 @font-face 挂字体文件上线**，`fonts/` 目录不得进入待发布仓库。
3. 用字体做出的**图片产出物**可自由发布（这是授权内的正宗用法）。

发布前检查：`code-backend/` 与 `code-frontend/` 的 `.gitignore` 必须挡住字体文件、证书、以及 `cache-tools/` 的本地产物。

## 7. 部署方案（已定，不得擅自更换）

- **GitHub Pages 源站 + Cloudflare DNS/CDN 代理**（不用 Cloudflare Pages）。
- GitHub Actions：采集 + 构建 + 部署在**同一个 workflow**；cron 每天 3 次、避开整点（UTC 03:17 / 07:17 / 11:17）。
- 技术栈：**Astro + Vue 岛**；真实路径路由，**禁用 hash 路由**。
- 数据双轨：构建期注入 JSON（SEO）+ 运行时比对 `version.json`（实时性）。

## 8. 工具分工（用户当前规划）

| 工具                 | 职责                                 |
| ------------------ | ---------------------------------- |
| Codex（GPT-6 Astra） | 主力开发（code-backend / code-frontend） |
| openCode           | 干不同事情时按任务选不同模型                     |
| WorkBuddy（Kimi-K3） | 全局梳理、文档维护                          |
| Cursor             | 样式微调等局部修改                          |

分工不是门禁：任何工具干活都必须遵守 §4–§7，越界改动（契约、部署方案、视觉定稿）一律先问用户。

## 9. 文档写作纪律

- 跨文档引用必须写文档全名 + 章节号（如 `` `cyber-granary-data-contract.md` §3.5 ``），不得裸写「契约 §x」。
- 事实性结论标注证据等级：`[实测]` / `[官方]` / `[推断]` / `[未定]`。
- 每条资讯类内容必须带可点击的原始源 URL；推广链接显式标注。
- 前端文档只写结构与文字，视觉细节（色值/字号/动效）是设计定稿的产出，不是文档的输入。
